{% extends 'base.html.twig' %}

{% block title %}Event
{% endblock %}

{% block body %}
	<h1>Event</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ event.id }}</td>
			</tr>
			<tr>
				<th>Date</th>
				<td>{{ event.date ? event.date|date('Y-m-d') : '' }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('admin_concert_index') }}">back to list</a>

	<a href="{{ path('admin_concert_edit', {'id': event.id}) }}">edit</a>

	{{ include('admin/concert/_delete_form.html.twig') }}
{% endblock %}
