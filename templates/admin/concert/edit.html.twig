{% extends 'base.html.twig' %}

{% block title %}Edition Concert
{% endblock %}

{% block body %}

	<div class="card my-4">
		<div class="card-body">
			<h5 class="card-title">Edition du concert :</h5>
			{{ include('admin/concert/_form.html.twig', {'button_label': 'Modifier'}) }}
		</div>
	</div>

	<div class='d-flex justify-content-end mt-4 '>
		{{ include('admin/concert/_delete_form.html.twig') }}
		<a class="btn btn-sm btn-secondary ms-2" href="{{ path('admin_concert_index') }}">Retour a la liste</a>
	</div>

{% endblock %}
